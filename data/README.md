# Data layout

- `Clean_Dataset.csv`, `business.csv`, `economy.csv` — raw files at repo root (and referenced from `config.yaml`).
- `data/reference/airports.csv` — location search + coordinates (not training labels).
- `data/processed/` — cleaned table, quality report, dataset decision JSON.
